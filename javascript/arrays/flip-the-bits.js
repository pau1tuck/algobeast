// Sample Input:
// 3 (q)
// 2147483647 (n1)
// 1 (n2)
// 0 (n3)
function flipBits(n) {
    // flipped = readlines 2-? and push to arr
    for (i = 1; i < q; i++) {
    const bits = arr[i].toString(2);
    const flippedBits = arr.reduce((acc, el), => { !(Number(el))) }
    console.log(flippedBits.parseInt() }